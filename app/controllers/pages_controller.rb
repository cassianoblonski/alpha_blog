class PagesController < ApplicationController

    def home
      # redirect the logged user to the articles page
      # redirect_to articles_path if logged_in?
      # I preffer to hide the sign up on the background
    end

    def about

    end


end
